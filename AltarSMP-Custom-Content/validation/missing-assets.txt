# Content the crossplay package cannot ship, and why.
# Generated by tools/crossplay/build_custom_content.py - do not edit by hand.

PAPER cmd None
    model data is computed at runtime at java:fabric-mod/src/main/java/com/altarsmp/fabric/weapon/s2/DragonrendWeapon.java:518:customModelData; the concrete values it can take are mapped individually
breeze_rod cmd 100
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season1[31]:Weapon Handle
clay_ball cmd 1 -> block/heavy_core
    the model resolves but none of its textures exist in either pack
clay_ball cmd 18
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season1[7]:Crazy Slots
clay_ball cmd 2 -> item/breeze_rod
    the item definition points at a model file that is not present in either source pack
echo_shard cmd 11
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season1[30]:Warden Head, content:items[10]:WardenHead
echo_shard cmd 25
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season1[34]:Pale Shard
ender_eye cmd 103
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season1[16]:Illusion Core
flint cmd 102
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season1[19]:Nightpiercer Shard
gold_nugget cmd 101
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season1[15]:Hyperion Shard
heavy_core cmd 999 -> block/clear
    the item definition points at a model file that is not present in either source pack
mace cmd 1
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season2[0]:Omen, content:weapons_s2[2]:omen
nether_star cmd 20
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:items[13]:WitherHeart
netherite_pickaxe cmd 0
    model data 0 means 'no custom model' - the vanilla item is shown; sources: content:altars/season1[6]:Copper Pickaxe Upgrade
netherite_sword cmd 0
    model data 0 means 'no custom model' - the vanilla item is shown; sources: content:altars/season1[12]:Fire Slash, content:weapons_s1[8]:fire_slash
recovery_compass cmd 2000
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season1[23]:Player Tracker, content:items[8]:PlayerTrackerItem
saddle cmd 1
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:items[16]:BlackGhastSaddle
snowball cmd 1 -> custom/ic
    the item definition points at a model file that is not present in either source pack
snowball cmd 2 -> custom/skull
    the item definition points at a model file that is not present in either source pack
snowball cmd 3 -> custom/redblock
    the item definition points at a model file that is not present in either source pack
totem_of_undying cmd 1026
    the mod can produce this pair but neither source pack defines a model for it, so there is no artwork to convert; sources: content:altars/season1[2]:Contagion Signal
unknown base cmd None
    model data is computed at runtime at java:fabric-mod/src/main/java/com/altarsmp/fabric/altar/AltarManager.java:184:cmd; the concrete values it can take are mapped individually
unknown base cmd None
    model data is computed at runtime at java:fabric-mod/src/main/java/com/altarsmp/fabric/item/ItemFactory.java:226:cmd; the concrete values it can take are mapped individually

# Java model states with no Bedrock equivalent:
altarsmp:frost [using_item]
    held-use state; approximated by the charge animation, not a second model
altarsmp:illusion_wand [using_item]
    held-use state; approximated by the charge animation, not a second model
altarsmp:vulcan [charge_type=arrow]
    crossbow charge type; Bedrock uses the vanilla charged geometry
altarsmp:vulcan [charge_type=rocket]
    crossbow charge type; Bedrock uses the vanilla charged geometry
altarsmp:vulcan [crossbow/pull>=0.58]
    state 'crossbow/pull>=0.58' has no Bedrock equivalent; the default model is used
altarsmp:vulcan [crossbow/pull>=1.0]
    state 'crossbow/pull>=1.0' has no Bedrock equivalent; the default model is used
altarsmp:vulcan [using_item]
    held-use state; approximated by the charge animation, not a second model
altarsmp:withersym [display_context=gui|ground|fixed]
    per-view model swap; Bedrock has one held model per attachable

# GeyserDisplayEntity note: item displays spawned by the mod (Displays.* call
# sites, tagged altarsmps2_vfx) are entity-side, not item-side. Geyser fetches
# their held item through the display entity's item component, so they render
# on Bedrock from the same mappings above; no extra assets are required.
